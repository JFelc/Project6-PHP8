
{% extends 'base.html.twig' %}
{% block body %}
<div class="container-fluid d-flex login-spread">
    <div class="signup-form align-self-center">
                <form method="post">
                    <fieldset>
                        <div class="form-group mb-3">
                            <label for="email">{{ 'Email'|trans }}</label>
                            <input type="email" name="email" class="form-control"/>
                        </div>
                        <div class="d-flex justify-content-center">
                        <button type="submit" class="btn btn-primary">
                            <i class="fa fa -sign-in" aria-hidden="true"></i> {{ 'Submit' |trans }}
                        </button>
                        </div>
                    </fieldset>
                </form>
                <div> 
    </div>
</div>
{% endblock %}